weather_project:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      project: bamboo-depth-486215-u4
      dataset: weather_analytics
      keyfile: "{{ env_var('GOOGLE_APPLICATION_CREDENTIALS') }}"
      location: EU
      threads: 4